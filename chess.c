#include <stdio.h>
#include "chess.h"

char** flipV(char** fig){
  char** = malloc(sizeof(fig));
  int j = 0;
  while(fig[i]){
    int i = 0;
    while(fig[j][i]){
      printf("%c ",fig[j][i]);
      i++;
    }
    printf("\n");
    j++;
  }
}
